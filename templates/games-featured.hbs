<div class="game-item">
    <div class="game-hero">
        <img src="{{this.mediaFiles.FEATURED_TWO_THIRDS.[840]}}">
    </div>
    <div class="container game-desc">
        <h1>{{this.name}}</h1>
        <h2>Strategy Games</h2>
        {{{this.description}}}
        <a href="{{this.gamePlayUrl}}" class="button"><span class="typcn typcn-media-play"></span>Play now</a>
    </div>
</div>